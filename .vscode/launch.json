{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Attach (pick process)",
      "type": "go",
      "request": "attach",
      "mode": "local",
      "processId": "${command:pickProcess}"
    }
  ]
}
